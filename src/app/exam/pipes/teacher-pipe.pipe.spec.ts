import { TeacherPipePipe } from './teacher-pipe.pipe';

describe('TeacherPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TeacherPipePipe();
    expect(pipe).toBeTruthy();
  });
});
